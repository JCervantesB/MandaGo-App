/**
 * Exports centralizados de assets (imágenes).
 * Uso: import { images } from '@/components/assets';
 *
 * Ejemplos de uso:
 *   - images.background
 *   - images.logo
 */
import background from '../../assets/background.png';
import logo from '../../assets/logo.png';

export const images = {
  background,
  logo,
};

export default { images };